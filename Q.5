Q5. Output of the code:

function fact(n)
 if n == 0
 return 1
 return n * fact(n - 1)
print(fact(4))

ans==24 , because n is never equal yo 0 while calculating in a stcak and as a rsult factorial of 4 is printed .
